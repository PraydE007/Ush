#include "../inc/ush.h"

// static void print_env(char **env) {
//     for (int i = 0; env[i]; i++) {
//         mx_printstr(env[i]);
//         mx_printchar('\n');
//     }
// }

void mx_env(char **command, char **env) {
    // bool P = 0;
    // bool i = 0;
    // bool u = 0;

    // if (command[1]) {
    //     for (int j = 1; command[1][j]; j++) {
    //         if (command[1][0] == '-') {
    //             if (command[j] == 'P')
    //                 P = 1;
    //             else if (command[j] == 'i')
    //                 i = 1;
    //             else if (command[j] == 'u')
    //                 u = 1;
    //         }
    //     }
    // }
    // if
    // print_env(env);
}
